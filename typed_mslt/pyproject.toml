[project]
name = "typed-mslt"
version = "0.1.0"
description = "Ontology-grounded typed dataflow from Japanese official-statistics CSVs to a multistate marital life table"
requires-python = ">=3.11"
dependencies = ["numpy>=1.24", "scipy>=1.10", "PyYAML>=6.0"]

[project.scripts]
mslt = "mslt.cli:main"
